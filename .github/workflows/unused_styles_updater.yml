name: Unused styles updater.

on:
  workflow_dispatch:

jobs:
  Unused-styles:
    runs-on: ubuntu-latest
    steps:
      - uses: desktop-app/action_code_updater@unused-styles
        with:
          type: "unused-styles"
